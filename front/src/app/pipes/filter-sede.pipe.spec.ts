import { FilterSedePipe } from './filter-sede.pipe';

describe('FilterSedePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSedePipe();
    expect(pipe).toBeTruthy();
  });
});
